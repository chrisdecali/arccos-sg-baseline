# --- credentials / tokens: NEVER commit ---
.arccos_creds.json
*arccos_creds*
.ghin_creds.json
*ghin_creds*

# --- 18Birdies raw export (contains email/name/friends/feed): keep the export out,
#     only the parsed 18birdies_rounds.csv (no PII) is committed ---
*18[Bb]irdies_archive*
*account*data*.json
.env
*.env
*token*
*.pem
*.key
*secret*

# --- raw API dumps (contain GPS coordinates + userId) ---
_discovery/
_cache_courses.json
_cache*

# --- shot GPS maps (KML/GeoJSON/CSV contain exact coordinates): LOCAL only ---
maps/

# --- misc ---
__pycache__/
*.pyc
*.log
_cron.log
